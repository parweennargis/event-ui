<!-- partial -->
<div class="main-panel">
  <div class="content-wrapper">
    <div class="page-header">
      <h3 class="page-title"> All Onsite Events </h3>
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="/admin/add-event">Create Onsite Event</a></li>
        </ol>
      </nav>
    </div>
    <div class="row">
      <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
          <div class="card-body">
            </p>
            <table class="table table-hover">
              <thead>
                <tr>
                  <th><b>Sl. No</b></th>
                  <th><b>Name</b></th>
                  <th><b>Date</b></th>
                  <th><b>Venue</b></th>
                  <th><b>Status</b></th>
                  <th><b>Action</b></th>
                  <th><b>Delete</b></th>
                </tr>
              </thead>
              <tbody>
                {{#each events}}
                <tr>
                  <td>{{_id}}</td>
                  <td>{{title}}</td>
                  <td>{{start_date}}</td>
                  <td>{{venue.name}}</td>
                  <td>{{is_active}}</td>

                  <td>
                    <a href="/admin/events/{{_id}}">
                      <div class="preview"> <i class="icon-pencil"></i> </div>
                    </a>
                  </td>
                  <td>
                    <a href="/admin/events/{{_id}}">
                      <div class="preview"> <i class="icon-trash"></i> </div>
                    </a>
                  </td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>